# 本地 AI 行研工作台：产品化方案

更新日期：2026-09-06

本文是待实施方案，不代表功能已经完成。目标用户是个人研究者及 AI 初学者；应用在本机启动、通过浏览器使用，GitHub 用于分发源码和展示成果。

详细开发规格、任务依赖与验收入口见 [开发文档索引](docs/README.md)。前端改版见 [前端设计规格](docs/05-FRONTEND.md)；原 Demo 仍在根目录 `index.html`。新版直接接入本地服务，未创建独立静态预览。

2026-09-06 开发更新：已直接实现连接真实本地存储的新版工作台，不再额外创建静态预览。入口为 `workbench.mjs`，当前状态和未完成项见 [实施记录](docs/IMPLEMENTATION.md)。下面保留原始方案作为目标，不代表所有计划均已完成。

## 产品定位

围绕材料行业的一个具体研究问题，将网页和用户提供的资料整理成可以查看原文依据、人工修改和持续更新的研究报告。

第一版继续聚焦电池材料，先完成一个真实案例。核心体验是：创建课题 → 确认研究问题 → 收集资料 → 提取证据 → 分章节写作 → 核对引用 → 编辑与导出。

本地运行不等于完全离线。第一版允许调用外部模型和搜索服务；项目与资料保存在本地，发送给外部服务的查询及资料片段需要在说明中明确。全离线模型不是第一版要求。

## 当前代码的实际情况

以下结论来自静态代码检查，尚未通过真实模型调用验证：

- `server.mjs`：已有 Node.js 服务、Bing RSS 搜索和可选的 OpenAI Responses API 调用。
- `src/app.js`：已有课题输入、报告编辑、来源列表、赛道评分、导出与 localStorage 保存。
- 报告上下文主要来自搜索摘要和内置来源说明，没有完整的原文证据处理流程。
- `fetchSource` 对已知来源直接返回内置说明；其他网页主要提取元描述，不应将其显示成已核验原文。
- 未配置密钥或部分失败路径会返回模板；前端请求失败会切换离线样例。产品版应明确区分演示、成功、部分完成和失败。
- 提示词要求 JSON，但没有严格输出结构校验；引用编号没有强制关联和验证。
- 赛道评分基于预置数值，不能当作模型完成真实研究后的评分。
- 研究状态保存在浏览器，没有独立项目库、报告版本和研究任务恢复机制。
- `.doc` 导出实际是 HTML 兼容文件，不是真正的 `.docx`。
- 当前目录未发现 README、依赖清单或 Git 元数据；目录还包含与行研无关的宠物图片产物，发布时应明确排除。

## 最小架构

保留现有页面和 Node.js 服务，按职责逐步拆文件，不先重写整个前端。

```text
浏览器工作台
    ↓
本机 Node.js 服务（仅监听 127.0.0.1）
    ├── 研究流程：计划、检索、证据、写作、检查
    ├── 模型与搜索调用
    └── SQLite 项目库 + 本地资料文件
```

- 先沿用现有模型接口，补齐超时、错误分类、结构校验和调用记录；需要第二家模型时再引入统一 SDK。
- SQLite 保存项目、资料索引、证据、报告版本和执行步骤；原文及附件单独存文件。
- 核心工作流使用明确的步骤状态和普通函数，每步保存输入、输出和结果。
- 仅在模型需要自主选择工具、根据证据缺口补充检索时加入有限循环，并设置调用次数和时间上限。
- TypeScript 可以先用于新加入的数据结构和核心模块，不要求一次性迁移所有代码。
- 第一版不安排账号、云端部署、微服务、独立向量数据库或桌面安装包。
- 本地服务仅开放需要的静态资源，不能直接暴露项目根目录；外部抓取限制协议、地址、重定向、体积与超时，避免访问本机或内网服务。

## 三个值得做实的技术亮点

### 1. 可追溯的证据与引用

每条证据保存来源 ID、URL 或文件名、抓取时间、原文片段、定位信息和内容哈希。网页定位使用章节和片段；PDF 能取得页码时保留页码。

报告中的关键事实关联证据 ID；点击引用能看到实际提取的原文。编号存在只能证明引用可定位，不能证明结论正确，因此还需要人工抽查“原文是否支持结论”。

行业数字额外记录时间、区域、单位、统计对象和来源；口径不同的数字先标记差异，避免直接判成事实冲突。

### 2. 可恢复的研究工作流

用户看到正在执行的步骤、工具输入输出摘要、耗时和错误。失败后能从已保存的步骤继续；修改某个章节时可以只重写该章节，保留人工修改的版本。

这套执行记录是可观察性，不展示或声称获取模型的内部思维过程。固定编排阶段称为工作流；实现自主工具选择后再描述为 Agent。

### 3. 可重复的质量评测

建立 10—20 个固定研究问题及人工核验的参考证据，保存模型、提示词版本、运行日期和调用用量。

统计引用可定位率、人工判定的证据支持率、无依据数字数量、完成率、耗时和调用用量。模型辅助审核只能作为提示，不能当作事实裁判。

只有取得实际结果后，才在 README 中写效果提升。对比时使用相同资料和问题，区分检索收益与写作收益。

## 实施顺序与验收

| 阶段 | 交付内容 | 完成标准 |
| --- | --- | --- |
| 1：可靠的本地产品底座 | 启动命令、配置示例、项目保存、明确错误状态、示例模式隔离、README | 重启后能恢复项目；没有密钥也能查看示例；调用失败不会伪装成研究成功；本地数据和密钥不会进入发布文件 |
| 2：可信研究闭环 | 网页正文提取、资料保存、结构化证据、分章节生成、引用检查、编辑与版本 | 一个真实材料案例的关键事实能回到原文；来源不足明确留空或待核验；修改后导出保留引用 |
| 3：可展示的研究过程 | 阶段进度、取消、有限重试、断点继续、调用记录、固定评测案例 | 重启或失败后能从已完成阶段继续；可查看用量和错误；评测结果能够复跑并说明差异 |
| 4：按使用反馈增强 | 文本型 PDF、资料问答、真正的 DOCX、多模型选择 | 由真实使用中的问题决定顺序；扫描 PDF 的 OCR 和复杂表格提取另列范围 |

本地资料问答先按章节组织文本并验证检索效果。SQLite FTS5 可以提供全文检索，但中文分词需单独验证；在中文研究集上测过后，再决定是否加入向量检索及结果融合。没有实现自主检索策略之前，不宣称 Agentic RAG。

## GitHub 展示

README 应包含：解决的问题、真实截图或演示、最少启动步骤、配置示例、架构图、一个脱敏研究案例、实测质量结果、已知限制和路线图。

提交前排除 `.env`、数据库、上传资料、运行日志、生成报告以及无关 `output` 产物。第三方报告全文默认不随代码分发，演示优先采用可再分发或自行编写的资料。开源许可证需要由项目所有者选定。

完成上述能力后，可以准确地介绍：

> 我做了一个本地 AI 行研工作台，将资料检索、结构化证据提取和分章节写作串成可恢复的流程，关键结论能回到原文，并用固定案例评估引用质量与调用用量。

比起技术名词数量，优先展示一份能经得起原文核对的报告，以及能够复现的运行记录。

## 已核对的官方技术资料

以下为参考文档地址，不作为当前功能已实现的证明；接入时仍需核对所选模型和版本的兼容性。

```text
OpenAI Structured Outputs
https://developers.openai.com/api/docs/guides/structured-outputs

AI SDK：统一模型调用与结构化生成
https://ai-sdk.dev/docs/introduction
https://ai-sdk.dev/docs/ai-sdk-core/generating-structured-data

SQLite FTS5：全文检索
https://sqlite.org/fts5.html
```
