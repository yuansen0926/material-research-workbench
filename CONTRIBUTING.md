# 开发约定

先阅读 `docs/IMPLEMENTATION.md` 中的实际状态，再阅读对应任务规格。

## 环境

Node.js 24.19+；`npm ci` 安装锁定依赖。

```text
npm test
npm run check
npm run eval
```

测试使用临时目录，不操作个人研究数据；默认不能执行真实模型调用。

## 修改边界

- `ui/`：正式前端；`src/` 是保留的旧 Demo。
- `server/`：本机服务、资料、模型和研究流程。
- `shared/contracts.mjs`：前后端共享字段与枚举。
- `fixtures/`：可分发的自编样例。
- `data/`：本地个人数据，不提交。

任何接口或存储变更同步更新实施记录。不要把样例回退写进真实研究错误路径。用户草稿、历史版本和原文引用必须可追溯。

## 提交前

核心测试通过，检查修改涉及的页面和窄屏布局。新模型或搜索服务需说明外部数据传输和配置边界。

提交说明应写清：具体问题、变化后的行为、如何验证、已知限制。没有跑过真实质量评测，不得加入准确率或效率提升宣称。
